# sample(seq, k)

import random
print("sample([1, 2, 3, 4], 2):", random.sample([1, 2, 3, 4], 2))
# → Output: list of k unique elements
# Returns k distinct random elements
# No duplicates allowed like in `choices`
