# choices(seq, k)

import random
print("choices([1, 2, 3], k=2):", random.choices([1, 2, 3], k=2))
# → Output: list of elements (duplicates allowed)
# Returns k random elements with repetition

'''
random.choices(population, weights=None, *, cum_weights=None, k=1)
'''
