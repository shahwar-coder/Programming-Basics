# uniform(a, b)

import random
print("uniform(1.5, 3.5):", random.uniform(1.5, 3.5))
# → Output: float between a and b
# Returns a random floating-point number

# Remember random.random(), also gives floating point number but in a fixed range of 0 and 1
