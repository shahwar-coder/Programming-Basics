'''
1. What is the one-line rule to remember?
Ans. `count()` tells how many times a value appears, `index()` tells where it appears first.
'''
# Example (lock it 🔒)
# count → quantity
# index → position

'''
2. built-ins:

len(), min(), max(), sum(), sorted(), tuple(), any(), all()
'''
